import { createTheme } from "@mui/material";

const theme = {
  typography: {
    fontSize: 23,
  },
  palette: {
    primary: {
      main: "#005d63",
    },
  },
};

export default createTheme(theme);
