import { Link } from "react-router-dom";

const Logo = () => {
    return (
        <Link to="/">
            <span className="font-itim text-[34px] font-semibold">Top Deal</span>
        </Link>
    );
};
export default Logo;
